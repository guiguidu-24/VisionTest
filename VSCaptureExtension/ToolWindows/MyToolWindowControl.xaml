<UserControl x:Class="VSCaptureExtension.MyToolWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:imaging="clr-namespace:Microsoft.VisualStudio.Imaging;assembly=Microsoft.VisualStudio.Imaging"
             xmlns:theming="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Imaging"
             xmlns:util="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Utilities"
             xmlns:catalog="clr-namespace:Microsoft.VisualStudio.Imaging;assembly=Microsoft.VisualStudio.ImageCatalog"
             xmlns:toolkit="clr-namespace:Community.VisualStudio.Toolkit;assembly=Community.VisualStudio.Toolkit" xmlns:vscaptureextension="clr-namespace:VSCaptureExtension" d:DataContext="{d:DesignInstance Type=vscaptureextension:MainViewModel}"
             toolkit:Themes.UseVsTheme="True"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="300"
             Name="MyToolWindow">
    <StackPanel>
        <StackPanel.Resources>
            <!-- Hide Image if CurrentScreenShot is null -->
            <Style TargetType="Image" x:Key="PreviewImage">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding CurrentScreenShot}" Value="{x:Null}">
                        <Setter Property="Visibility" Value="Collapsed" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </StackPanel.Resources>

        <!-- Top-left corner button with "+" icon -->
        <Button x:Name="AddButton" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10" Height="30" Padding="5" Command="{Binding ClickAddCommand}">
            <Button.Content>
                <StackPanel Orientation="Horizontal">
                    <Image Source="/VSCaptureExtension;component/Resources/Add.png" Stretch="Fill" Width="auto" Height="auto" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="Add" VerticalAlignment="Center"/>
                </StackPanel>
            </Button.Content>
        </Button>

        <Image Grid.Column="1" Style="{StaticResource PreviewImage}" Source="{Binding CurrentScreenShot}" Stretch="Uniform" HorizontalAlignment="Center" MaxHeight="100" MinHeight="40"/>

        <!-- Checkboxes and TextBox -->
        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <CheckBox Grid.Row="0" x:Name="TextCheckBox" Content="Text" Margin="0,5,0,0"/>
            <!-- TextBox below the Text checkbox -->
            <TextBox Grid.Row="1" Text="Text to find..." Margin="0,5,0,0" Width="200" Height="auto" />
            <TextBlock Grid.Row="1" Grid.Column="1" VerticalAlignment="Bottom" HorizontalAlignment="Right" Text="{Binding TextFound}"/>
            <TextBox Grid.Row="1" Grid.Column="2" x:Name="FoundTextBox" Text="Text found" Margin="0,5,0,0" Width="200" Height="auto" />
            <CheckBox Grid.Row="2" x:Name="ImageCheckBox" Content="Image" Margin="0,5,0,0"/>
            <CheckBox Grid.Row="3" x:Name="ColorCheckBox" Content="Color" Margin="0,5,0,0"/>
        </Grid>
    </StackPanel>
</UserControl>
