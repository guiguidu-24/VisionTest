<UserControl x:Class="VSCaptureExtension.MyToolWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:imaging="clr-namespace:Microsoft.VisualStudio.Imaging;assembly=Microsoft.VisualStudio.Imaging"
             xmlns:theming="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Imaging"
             xmlns:util="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Utilities"
             xmlns:catalog="clr-namespace:Microsoft.VisualStudio.Imaging;assembly=Microsoft.VisualStudio.ImageCatalog"
             xmlns:toolkit="clr-namespace:Community.VisualStudio.Toolkit;assembly=Community.VisualStudio.Toolkit"
             toolkit:Themes.UseVsTheme="True"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="300"
             Name="MyToolWindow">
    <Grid>
        <!-- Top-left corner button with "+" icon -->
        <Button x:Name="AddButton" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10" Height="30" Padding="5">
            <Button.Content>
                <StackPanel Orientation="Horizontal">
                    <Image Source="/VSCaptureExtension;component/Resources/Add.png" Stretch="Fill" Width="auto" Height="auto" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="Add" VerticalAlignment="Center"/>
                </StackPanel>
            </Button.Content>
        </Button>

        <!-- Checkboxes and TextBox -->
        <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,50,0,0">
            <CheckBox x:Name="TextCheckBox" Content="Text" Margin="0,5,0,0"/>
            <!-- TextBox below the Text checkbox -->
            <TextBox x:Name="PreviewTextBox" Text="Text to find..." Margin="0,5,0,0" Width="200" Height="auto" />
            <CheckBox x:Name="ImageCheckBox" Content="Image" Margin="0,5,0,0"/>
            <CheckBox x:Name="ColorCheckBox" Content="Color" Margin="0,5,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
